export class ArucoDetector {
  constructor({ markerId }) {
    this.markerId = markerId;
  }

  detect(_imageDataOrCanvas) {
    // Placeholder for js-aruco2 integration in the next milestone.
    return null;
  }
}
